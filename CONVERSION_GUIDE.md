# دليل تحويل الكتب الإلكترونية

## 📚 نظرة عامة

هذا الدليل يشرح كيفية تحويل ملفات EPUB و KFX إلى نصوص عربية.

---

## 🔧 المتطلبات

### 1. مكتبات Python المطلوبة

```bash
pip install ebooklib beautifulsoup4 lxml
```

### 2. Calibre (اختياري لكن موصى به)

**لتحويل EPUB و KFX بجودة أفضل:**

1. قم بتنزيل وتثبيت Calibre من: https://calibre-ebook.com/
2. تأكد من إضافة Calibre إلى PATH

**للتحقق من التثبيت:**
```bash
ebook-convert --version
```

### 3. لملفات KFX المحمية بـ DRM

إذا كانت ملفات KFX محمية، ستحتاج إلى:
- تثبيت Calibre
- تثبيت DeDRM plugin من: https://github.com/noDRM/DeDRM_tools

---

## 🚀 الاستخدام

### الطريقة الأولى: تشغيل السكريبت مباشرة

```bash
cd E:\arabic_novel_dataset\Al-Nass-Extractor
python scripts\converters\batch_ebook_converter.py
```

### الطريقة الثانية: استيراد كوحدة

```python
from scripts.converters.batch_ebook_converter import EbookConverter

# تحديد المجلدات
input_dir = r"E:\arabic_novel_dataset\Al-Nass-Extractor\input for convert"
output_dir = r"E:\arabic_novel_dataset\Al-Nass-Extractor\input"

# إنشاء المحول
converter = EbookConverter(input_dir, output_dir)

# تحويل جميع الملفات
results = converter.convert_all()
```

### الطريقة الثالثة: تحويل ملف واحد

```python
from pathlib import Path
from scripts.converters.batch_ebook_converter import EbookConverter

converter = EbookConverter(input_dir, output_dir)

# تحويل ملف واحد
file_path = Path("E:/arabic_novel_dataset/Al-Nass-Extractor/input for convert/السكرية.epub")
result = converter.convert_file(file_path)
```

---

## 📊 المخرجات

لكل كتاب، يتم إنشاء ملفين:

### 1. ملف TXT
```
اسم_الكتاب.txt
```
نص عادي يحتوي على محتوى الكتاب

### 2. ملف JSON
```
اسم_الكتاب.txt.json
```
بنية JSON متوافقة مع التطبيق:
```json
{
  "pages": [
    {
      "page": 1,
      "text": "نص الكتاب..."
    }
  ],
  "metadata": {
    "source_file": "السكرية.epub",
    "conversion_method": "ebooklib",
    "word_count": 125000,
    "character_count": 650000
  }
}
```

### 3. تقرير التحويل
```
conversion_report.json
```
يحتوي على ملخص لجميع عمليات التحويل

---

## 🎯 الملفات في المجلد

### الكتب الموجودة:

**ملفات EPUB (22 ملف):**
- أولاد_حارتنا.epub
- الحرافيش (3).epub
- السكرية.epub
- السمان_والخريف.epub
- الشحاذ.epub
- الطريق.epub
- القاهرة_الجديدة.epub
- ثرثرة_فوق_النيل.epub
- زقاق_المدق.epub
- قصر_الشوق.epub
- همس_الجنون.epub

**ملفات KFX (11 ملف):**
- أولاد_حارتنا.kfx
- الحرافيش (1).kfx
- السكرية (1).kfx
- السمان_والخريف.kfx
- الشحاذ.kfx
- الطريق.kfx
- القاهرة_الجديدة.kfx
- ثرثرة_فوق_النيل.kfx
- زقاق_المدق.kfx
- قصر_الشوق.kfx
- همس_الجنون.kfx

---

## ⚙️ طرق التحويل

### 1. ebooklib (للـ EPUB)
- سريع وبسيط
- لا يتطلب برامج خارجية
- مناسب لمعظم ملفات EPUB

### 2. Calibre (للـ EPUB و KFX)
- جودة أفضل
- يدعم KFX
- يحتاج Calibre مثبت

---

## 🔍 استكشاف الأخطاء

### المشكلة: "ebooklib not found"
**الحل:**
```bash
pip install ebooklib beautifulsoup4 lxml
```

### المشكلة: "ebook-convert not found"
**الحل:**
- قم بتثبيت Calibre
- أضف مسار Calibre إلى PATH

### المشكلة: ملفات KFX لا يمكن قراءتها
**الحل:**
- تأكد من تثبيت Calibre
- قد تحتاج DeDRM plugin إذا كانت الملفات محمية

### المشكلة: نص غير مقروء أو مشوّه
**الحل:**
- جرب طريقة Calibre بدلاً من ebooklib
- تأكد من ترميز UTF-8

---

## 📝 ملاحظات

1. **الأولوية:** يحاول السكريبت ebooklib أولاً، ثم Calibre إذا فشل
2. **EPUB vs KFX:** EPUB أسهل في التحويل. KFX يحتاج Calibre
3. **الجودة:** Calibre يعطي جودة أفضل عموماً
4. **السرعة:** ebooklib أسرع لكن أقل موثوقية أحياناً
5. **DRM:** الملفات المحمية تحتاج DeDRM plugin

---

## ✅ خطوات سريعة

```bash
# 1. تثبيت المكتبات
pip install ebooklib beautifulsoup4 lxml

# 2. (اختياري) تثبيت Calibre
# قم بتنزيله من https://calibre-ebook.com/

# 3. تشغيل المحول
python scripts\converters\batch_ebook_converter.py

# 4. تحقق من المخرجات في مجلد input/
```

---

## 📞 الدعم

للمشاكل أو الأسئلة، راجع التوثيق الرئيسي في [README_COMPREHENSIVE.md](README_COMPREHENSIVE.md)

**تم إنشاؤه**: 15 يناير 2026
